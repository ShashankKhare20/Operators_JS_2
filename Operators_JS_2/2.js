// In JavaScript, the comma operator allows you to evaluate multiple expressions within a single statement. It is used to separate expressions and returns the value of the last expression.


var x
var y

function product(x,y) {
    console.log(`The product is ${x*y}`)
}

product(10,5)